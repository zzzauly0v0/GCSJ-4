# 地名 → 158 站点/区县 离线匹配 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `resolve_place` 改为先用库内 158 气象站名 + 四川行政区划名离线匹配地名，未命中才回退 geopy Nominatim，并校验回退坐标是否落在四川。

**Architecture:** 新增 `tools/db.py` 集中 DSN 与连接；`geo.py` 新增纯函数 `match_place` 与候选加载 `load_place_candidates`，重写 `resolve_place(place, geocode=None, candidates=None)`——candidates 缺省时从库加载，命中则离线返回，否则回退 Nominatim + 四川 bbox 校验。`gis_query.py` 改用 `db.py` 的 DSN。

**Tech Stack:** Python 3, psycopg2, geopy, pytest, openai-agents (`@function_tool`)。

## Global Constraints

- 所有源码/注释沿用现有中文风格与简洁注释密度。
- 数据库连接串统一为 `host=localhost port=5432 dbname=gcsj user=gcsj password=gcsj123`。
- `resolve_place` 现有返回字段 `found / name / lon / lat / message` 语义不变；新增 `source`、`in_sichuan`。
- 四川 bbox: `lon ∈ [97.3, 108.6]`, `lat ∈ [26.0, 34.4]`。
- 纯函数保持可注入（geocode、candidates）以便无 DB 测试。
- 测试命令统一从仓库根运行: `spatial_analyse/.venv/Scripts/python.exe -m pytest ...`（conftest 已把根目录加入 sys.path）。

---

### Task 1: 抽取共享 DB 模块

**Files:**
- Create: `spatial_analyse/tools/db.py`
- Modify: `spatial_analyse/tools/gis_query.py:9`（删除本地 `DB_DSN`，改为从 db 引入）

**Interfaces:**
- Produces: `DB_DSN: str`、`connect(dsn: str = DB_DSN)`（返回 psycopg2 连接）。
- Consumes: 无。

- [ ] **Step 1: 写 db.py**

```python
"""共享数据库连接: 统一 DSN 与连接创建, 避免各工具重复。"""
import psycopg2

DB_DSN = "host=localhost port=5432 dbname=gcsj user=gcsj password=gcsj123"


def connect(dsn: str = DB_DSN):
    """创建 psycopg2 连接 (调用方负责 close)。"""
    return psycopg2.connect(dsn)
```

- [ ] **Step 2: gis_query.py 改用共享 DSN**

把 `gis_query.py` 顶部的

```python
DB_DSN = "host=localhost port=5432 dbname=gcsj user=gcsj password=gcsj123"
```

改为

```python
from spatial_analyse.tools.db import DB_DSN
```

（`nearest_station` / `_fetch_eval_rows` 仍保留 `dsn: str = DB_DSN` 参数，其余不动。）

- [ ] **Step 3: 跑现有测试确认未回归**

Run: `spatial_analyse/.venv/Scripts/python.exe -m pytest spatial_analyse/tests/test_tools.py -v`
Expected: PASS（现有 7 个用例全过，import 不报错）。

- [ ] **Step 4: Commit**

```bash
git add spatial_analyse/tools/db.py spatial_analyse/tools/gis_query.py
git commit -m "refactor(ai): 抽取 tools/db.py 共享 DSN, gis_query 改用之"
```

---

### Task 2: `match_place` 纯匹配函数

**Files:**
- Modify: `spatial_analyse/tools/geo.py`（新增 `match_place`）
- Test: `spatial_analyse/tests/test_tools.py`（新增用例）

**Interfaces:**
- Consumes: 无（纯函数）。
- Produces:
  `match_place(place: str, candidates: list[dict]) -> dict | None`
  - candidate 结构: `{"name": str, "lon": float, "lat": float, "source": "station"|"region", "level": int|None}`
  - 返回命中的 candidate（原样，含其 name/lon/lat/source/level）或 `None`。

- [ ] **Step 1: 写失败测试**

在 `test_tools.py` 顶部 import 增补:

```python
from spatial_analyse.tools.geo import match_place
```

追加用例:

```python
def _cands():
    return [
        {"name": "成都", "lon": 104.07, "lat": 30.67, "source": "region", "level": 2},
        {"name": "都江堰", "lon": 103.62, "lat": 30.99, "source": "region", "level": 3},
        {"name": "九寨沟", "lon": 103.92, "lat": 33.26, "source": "station", "level": None},
        {"name": "峨眉山", "lon": 103.48, "lat": 29.60, "source": "station", "level": None},
    ]


def test_match_place_exact():
    r = match_place("成都", _cands())
    assert r["name"] == "成都" and r["source"] == "region"


def test_match_place_station_contains():
    # 站名被包含在 query 中
    r = match_place("我想去峨眉山玩", _cands())
    assert r["name"] == "峨眉山" and r["source"] == "station"


def test_match_place_prefers_more_specific():
    # 同时含 成都(市级) 与 都江堰(县级) -> 取更具体的县/站
    r = match_place("成都都江堰", _cands())
    assert r["name"] == "都江堰"


def test_match_place_no_match():
    assert match_place("上海东方明珠", _cands()) is None
```

- [ ] **Step 2: 跑测试确认失败**

Run: `spatial_analyse/.venv/Scripts/python.exe -m pytest spatial_analyse/tests/test_tools.py -k match_place -v`
Expected: FAIL（`ImportError: cannot import name 'match_place'`）。

- [ ] **Step 3: 实现 match_place**

在 `geo.py` `import json` 下方新增（放在 `resolve_place` 之前）:

```python
# 行政区划名归一化: 去掉这些后缀后再比较
_REGION_SUFFIXES = ("自治州", "省", "市", "州", "县", "区")


def _norm_region(name: str) -> str:
    """行政区划名去后缀 (自治州优先, 避免只去掉'州')。"""
    for suf in _REGION_SUFFIXES:
        if name.endswith(suf) and len(name) > len(suf):
            return name[: -len(suf)]
    return name


def _specificity(cand: dict) -> int:
    """平局时取更具体者: 站点最具体, 其次行政级别数字越大越具体。"""
    if cand["source"] == "station":
        return 100
    return int(cand.get("level") or 0)


def match_place(place: str, candidates: list) -> dict:
    """把地名匹配到库内站点/区县候选, 命中返回该 candidate, 否则 None (纯函数)。

    优先级: 精确等名 > 包含匹配; 同优先级下取更具体者 (站点 > 县 > 市 > 省)。
    """
    q = (place or "").strip()
    if not q:
        return None

    exact, contains = [], []
    for c in candidates:
        raw = c["name"].strip()
        key = _norm_region(raw) if c["source"] == "region" else raw
        if not key:
            continue
        if q == raw or q == key:
            exact.append(c)
        elif key in q or q in key:
            contains.append(c)

    pool = exact or contains
    if not pool:
        return None
    return max(pool, key=_specificity)
```

- [ ] **Step 4: 跑测试确认通过**

Run: `spatial_analyse/.venv/Scripts/python.exe -m pytest spatial_analyse/tests/test_tools.py -k match_place -v`
Expected: PASS（4 个用例全过）。

- [ ] **Step 5: Commit**

```bash
git add spatial_analyse/tools/geo.py spatial_analyse/tests/test_tools.py
git commit -m "feat(ai): 新增 match_place 地名->站点/区县离线匹配纯函数"
```

---

### Task 3: 候选加载 `load_place_candidates`

**Files:**
- Modify: `spatial_analyse/tools/geo.py`（新增 `load_place_candidates`）

**Interfaces:**
- Consumes: `db.connect`、`db.DB_DSN`（Task 1）。
- Produces:
  `load_place_candidates(dsn: str = DB_DSN) -> list[dict]`
  返回 station + region 候选列表（结构同 Task 2 candidate）。

- [ ] **Step 1: 实现 load_place_candidates**

在 `geo.py` import 段增补:

```python
from spatial_analyse.tools.db import DB_DSN, connect
```

在 `match_place` 之后新增:

```python
def load_place_candidates(dsn: str = DB_DSN) -> list:
    """从库里加载站点名 + 行政区划名候选 (含经纬度)。

    station 用 location 点, region 用 center 点。center 为空的区划跳过。
    """
    sql = """
        SELECT name,
               ST_X(location) AS lon, ST_Y(location) AS lat,
               'station' AS source, NULL::int AS level
        FROM gis.gis_weather_station
        WHERE location IS NOT NULL
        UNION ALL
        SELECT name,
               ST_X(center) AS lon, ST_Y(center) AS lat,
               'region' AS source, level
        FROM gis.gis_admin_region
        WHERE center IS NOT NULL
    """
    import psycopg2.extras
    conn = connect(dsn)
    try:
        with conn.cursor(cursor_factory=psycopg2.extras.RealDictCursor) as cur:
            cur.execute(sql)
            rows = cur.fetchall()
    finally:
        conn.close()
    return [
        {"name": r["name"], "lon": float(r["lon"]), "lat": float(r["lat"]),
         "source": r["source"], "level": r["level"]}
        for r in rows
    ]
```

- [ ] **Step 2: 语法/import 冒烟检查**

Run: `spatial_analyse/.venv/Scripts/python.exe -c "from spatial_analyse.tools.geo import load_place_candidates; print('ok')"`
Expected: 打印 `ok`（无 import 错误；此函数需 DB 才能真正执行，这里只验证可导入）。

- [ ] **Step 3: Commit**

```bash
git add spatial_analyse/tools/geo.py
git commit -m "feat(ai): geo 新增 load_place_candidates 从库加载站点/区划候选"
```

---

### Task 4: 重写 `resolve_place`（离线优先 + 四川校验）

**Files:**
- Modify: `spatial_analyse/tools/geo.py`（重写 `resolve_place`）
- Modify: `spatial_analyse/tests/test_tools.py`（更新 1 个旧用例 + 新增用例）

**Interfaces:**
- Consumes: `match_place`、`load_place_candidates`（Task 2/3）。
- Produces:
  `resolve_place(place: str, geocode=None, candidates=None) -> dict`
  返回 `{found, name, lon, lat, source, in_sichuan, message}`。
  - candidates 为 None → 调 `load_place_candidates()`；传入 list（含空 list）→ 直接用，不查库。

- [ ] **Step 1: 更新旧用例 + 写新失败测试**

把 `test_tools.py` 里现有 `test_resolve_place_found` 改为传空候选强制回退:

```python
def test_resolve_place_found():
    # 传空候选 -> 强制走 geocode 回退; 九寨沟坐标在四川内
    def fake_geocode(q, **kw):
        return _FakeLoc(103.918, 33.262)

    r = resolve_place("九寨沟", geocode=fake_geocode, candidates=[])
    assert r["found"] is True
    assert abs(r["lon"] - 103.918) < 1e-6
    assert abs(r["lat"] - 33.262) < 1e-6
    assert r["name"] == "九寨沟"
    assert r["source"] == "nominatim"
    assert r["in_sichuan"] is True
```

把现有 `test_resolve_place_not_found` 也补 `candidates=[]`:

```python
def test_resolve_place_not_found():
    def fake_geocode(q, **kw):
        return None

    r = resolve_place("不存在的地方xyz", geocode=fake_geocode, candidates=[])
    assert r["found"] is False
    assert r["lon"] is None
    assert "未" in r["message"] or "not" in r["message"].lower()
```

追加新用例:

```python
def test_resolve_place_offline_hit_does_not_call_geocode():
    called = {"n": 0}

    def boom_geocode(q, **kw):
        called["n"] += 1
        raise AssertionError("不该调用 geocode")

    cands = [{"name": "都江堰", "lon": 103.62, "lat": 30.99,
              "source": "region", "level": 3}]
    r = resolve_place("成都都江堰", geocode=boom_geocode, candidates=cands)
    assert r["found"] is True
    assert r["source"] == "region"
    assert r["in_sichuan"] is True
    assert abs(r["lon"] - 103.62) < 1e-6
    assert called["n"] == 0


def test_resolve_place_fallback_outside_sichuan():
    # 回退命中但坐标在上海 (出川) -> found=False 提示补充
    def fake_geocode(q, **kw):
        return _FakeLoc(121.47, 31.23)

    r = resolve_place("某模糊地名", geocode=fake_geocode, candidates=[])
    assert r["found"] is False
    assert r["in_sichuan"] is False
    assert "四川" in r["message"] or "具体" in r["message"]


def test_resolve_place_geocode_exception():
    def bad_geocode(q, **kw):
        raise RuntimeError("限流")

    r = resolve_place("某地", geocode=bad_geocode, candidates=[])
    assert r["found"] is False
    assert "异常" in r["message"] or "限流" in r["message"]
```

- [ ] **Step 2: 跑测试确认失败**

Run: `spatial_analyse/.venv/Scripts/python.exe -m pytest spatial_analyse/tests/test_tools.py -k resolve_place -v`
Expected: FAIL（新字段 `source`/`in_sichuan` 缺失、出川校验未实现）。

- [ ] **Step 3: 重写 resolve_place**

用下列实现替换 `geo.py` 中现有 `resolve_place` 整个函数:

```python
# 四川 bbox (宽松包络): 用于校验回退坐标是否在川
_SC_LON = (97.3, 108.6)
_SC_LAT = (26.0, 34.4)


def _in_sichuan(lon: float, lat: float) -> bool:
    return _SC_LON[0] <= lon <= _SC_LON[1] and _SC_LAT[0] <= lat <= _SC_LAT[1]


def resolve_place(place: str, geocode=None, candidates=None) -> dict:
    """地名 -> 经纬度: 先库内站点/区划离线匹配, 未命中回退 Nominatim 并校验在川。

    candidates: None 则从库加载; 传 list (含空 list) 则直接用, 不查库 (便于测试)。
    geocode: 可调用, 入参 (query, **kw), 返回带 .longitude/.latitude 的对象或 None。
    """
    # 1) 离线匹配库内候选
    if candidates is None:
        try:
            candidates = load_place_candidates()
        except Exception:
            candidates = []
    hit = match_place(place, candidates)
    if hit is not None:
        return {"found": True, "name": place,
                "lon": float(hit["lon"]), "lat": float(hit["lat"]),
                "source": hit["source"], "in_sichuan": True, "message": "ok"}

    # 2) 回退 Nominatim
    if geocode is None:
        from geopy.geocoders import Nominatim
        geocode = Nominatim(user_agent="gcsj-disaster-agent", timeout=10).geocode

    try:
        loc = geocode(place)
    except Exception as e:  # 网络/限流等
        return {"found": False, "name": place, "lon": None, "lat": None,
                "source": None, "in_sichuan": False,
                "message": f"定位服务异常: {e}"}

    if loc is None:
        return {"found": False, "name": place, "lon": None, "lat": None,
                "source": None, "in_sichuan": False,
                "message": f"未找到地点「{place}」, 请换个更具体的说法 (如加上省市)。"}

    lon, lat = float(loc.longitude), float(loc.latitude)
    if not _in_sichuan(lon, lat):
        return {"found": False, "name": place, "lon": None, "lat": None,
                "source": None, "in_sichuan": False,
                "message": f"地点「{place}」似乎不在四川范围内, 请补充更具体的四川地名。"}

    return {"found": True, "name": place, "lon": lon, "lat": lat,
            "source": "nominatim", "in_sichuan": True, "message": "ok"}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `spatial_analyse/.venv/Scripts/python.exe -m pytest spatial_analyse/tests/test_tools.py -v`
Expected: PASS（全部用例，含 resolve_place 5 个 + match_place 4 个 + 原有 disaster/knowledge 用例）。

- [ ] **Step 5: Commit**

```bash
git add spatial_analyse/tools/geo.py spatial_analyse/tests/test_tools.py
git commit -m "feat(ai): resolve_place 离线优先匹配站点/区划 + Nominatim 兜底校验在川"
```

---

### Task 5: 端到端冒烟（有 DB 时）+ 系统提示微调

**Files:**
- Modify: `spatial_analyse/agent_core.py`（可选：系统提示提及"就近站点"仍准确，一般无需改）

**Interfaces:**
- Consumes: `resolve_place`（Task 4）、`load_place_candidates`（Task 3）。
- Produces: 无新接口。

- [ ] **Step 1: 有 DB 时验证离线匹配真跑通**

Run: `spatial_analyse/.venv/Scripts/python.exe -c "from spatial_analyse.tools.geo import resolve_place; print(resolve_place('都江堰'))"`
Expected: 打印 `found=True`、`source` 为 `station` 或 `region`、`in_sichuan=True`、`lon/lat` 在四川范围。
（若本机无 DB，`load_place_candidates` 异常被吞为空候选 → 回退 geocode；此步需在有 DB 环境执行。）

- [ ] **Step 2: 确认 agent_core 无需改动**

阅读 [agent_core.py](../../../spatial_analyse/agent_core.py) 的 `_SYSTEM_PROMPT`：其描述 geo_locate「把地名解析为经纬度」仍准确，无需改。若发现措辞与新行为冲突再改；否则跳过。

- [ ] **Step 3: 全量测试收尾**

Run: `spatial_analyse/.venv/Scripts/python.exe -m pytest spatial_analyse/tests/ -v`
Expected: 全 PASS。

- [ ] **Step 4: Commit（仅当有改动）**

```bash
git add -A spatial_analyse/
git commit -m "chore(ai): 地名离线匹配端到端冒烟收尾"
```

---

## Self-Review

**Spec coverage:**
- 站名 + 区划名双数据源 → Task 3 `load_place_candidates`（UNION）。✓
- 归一化去后缀 → Task 2 `_norm_region`。✓
- 优先级 精确>包含、平局取更具体（站>县>市>省）→ Task 2 `_specificity` + pool 逻辑。✓
- 先库内、未命中回退 Nominatim → Task 4。✓
- 四川 bbox 校验出川 found=False → Task 4 `_in_sichuan`。✓
- 返回结构向后兼容 + 新增 source/in_sichuan → Task 4。✓
- `query_station_disasters` 不改 → 计划未触碰其逻辑（仅 Task 1 换 DSN import）。✓
- db.py 消除 DSN 重复 → Task 1。✓
- 测试覆盖 match_place 5 路径 + resolve_place 4 路径 → Task 2/4。✓

**Placeholder scan:** 无 TBD/TODO；每个代码步骤含完整代码。✓

**Type consistency:** candidate 结构 `{name,lon,lat,source,level}` 在 Task 2/3/4 一致；`match_place` 返回 candidate dict，`resolve_place` 读 `hit["lon"]/["lat"]/["source"]` 一致；`_specificity` 读 `source`/`level` 与 candidate 一致。✓