# AI 灾害助手页面 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 删除空的 `/agent` 路由占位, 重建为「AI 灾害助手」页面 — 用户用自然语言问出行安全, Agent 综合地理定位 + 就近气象站历史灾害 + 静态科普, 流式产出针对性出行风险研判。

**Architecture:** Python FastAPI 独立服务 (`:8000`) 承载 Agent, 用 OpenAI Agents SDK 的 `@function_tool` 定义 geo_locate / query_station_disasters / disaster_kb 三个工具, Agent 自动规划调用。前端 `AgentView.vue` 用原生 fetch 读 SSE 流式渲染, 经 Vite proxy `/ai` 直连 Python, 与 Java 后端 (`:8085`) 解耦。

**Tech Stack:** Python 3.12 + FastAPI + uvicorn + openai-agents SDK + psycopg2 + geopy; 阿里云 DashScope (Qwen, OpenAI 兼容); Vue 3 + Element Plus + 原生 fetch/SSE。

## Global Constraints

- Python 版本: `>=3.12` (见 `spatial_analyse/.python-version` = 3.12)
- LLM 接入: DashScope OpenAI 兼容端点 `https://dashscope.aliyuncs.com/compatible-mode/v1`, 模型/Key 从 `.env` 读 `DASHSCOPE_MODEL_ID` / `DASHSCOPE_API_KEY` (沿用现有 `agent.py` 约定)
- 数据库: `DB_DSN = "host=localhost port=5432 dbname=gcsj user=gcsj password=gcsj123"` (沿用 `run_disaster_eval.py`)
- Python 服务端口: `8000`
- 前端图标必须是合法 Element Plus 图标名 (菜单用 `<component :is="child.icon">` 动态渲染, 非法名会崩) — 用 `ChatDotRound`
- 文件名大小写: 新建 `AgentView.vue` (大写 V), 与 import 路径严格一致 (Windows 大小写坑)
- SSE 帧格式: 每帧 `data: {json}\n\n`, json 含 `type` 字段 ∈ {`tool`, `token`, `done`, `error`}
- 依赖管理: 用 `uv` (项目已有 `uv.lock`)

---

## 文件结构

**新建 (Python):**
- `spatial_analyse/tools/__init__.py` — 包标记
- `spatial_analyse/tools/geo.py` — `geo_locate` 工具 + 纯函数 `resolve_place`
- `spatial_analyse/tools/gis_query.py` — `query_station_disasters` 工具 + 纯函数 `nearest_station` / `summarize_disasters`
- `spatial_analyse/tools/knowledge.py` — `disaster_kb` 工具 + 静态知识常量
- `spatial_analyse/knowledge/` — 静态科普 Markdown (暴雨/滑坡/泥石流/冻融/崩塌)
- `spatial_analyse/agent_core.py` — 组装 Agent + 系统提示词 + 流式运行器
- `spatial_analyse/api_server.py` — FastAPI 应用 (`POST /ai/chat`, `GET /ai/health`)
- `spatial_analyse/tests/test_tools.py` — 纯函数单测

**新建 (前端):**
- `frontend/src/api/agent.js` — SSE 流式客户端
- `frontend/src/views/agent/AgentView.vue` — 聊天页面

**修改:**
- `spatial_analyse/pyproject.toml` — 加 fastapi/uvicorn/openai-agents 依赖 + pytest dev 依赖
- `frontend/src/router/index.js:26-30` — 修 component 路径 + icon
- `frontend/vite.config.js` — proxy 加 `/ai`

**删除:**
- `frontend/src/views/agent/agentView.vue` (0 字节空文件, 小写 v)

---

## Task 1: Python 依赖与工程骨架

**Files:**
- Modify: `spatial_analyse/pyproject.toml`
- Create: `spatial_analyse/tools/__init__.py`
- Create: `spatial_analyse/tests/__init__.py`

**Interfaces:**
- Produces: 可 import 的 `spatial_analyse.tools` 包; `pytest` / `uvicorn` / `openai-agents` 可用。

- [ ] **Step 1: 更新 pyproject.toml 依赖**

把 `spatial_analyse/pyproject.toml` 的 `dependencies` 段替换为 (在原有基础上追加三项), 并新增 dev 依赖:

```toml
[project]
name = "spatial-analyse"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "geopy>=2.4.1",
    "pandas>=3.0.3",
    "pyproj>=3.7.2",
    "psycopg2-binary>=2.9.9",
    "requests>=2.32.0",
    "shapely>=2.0.0",
    "openai>=2.44.0",
    "dotenv>=0.9.9",
    "fastapi>=0.115.0",
    "uvicorn>=0.30.0",
    "openai-agents>=0.2.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]
```

- [ ] **Step 2: 创建包标记文件**

`spatial_analyse/tools/__init__.py`:
```python
```
(空文件)

`spatial_analyse/tests/__init__.py`:
```python
```
(空文件)

- [ ] **Step 3: 安装依赖**

Run: `cd spatial_analyse && uv sync`
Expected: 成功解析并安装 fastapi / uvicorn / openai-agents / pytest, 无冲突。

- [ ] **Step 4: 验证关键库可导入**

Run: `cd spatial_analyse && uv run python -c "import fastapi, uvicorn; from agents import Agent, function_tool, Runner; print('ok')"`
Expected: 输出 `ok`

- [ ] **Step 5: Commit**

```bash
git add spatial_analyse/pyproject.toml spatial_analyse/uv.lock spatial_analyse/tools/__init__.py spatial_analyse/tests/__init__.py
git commit -m "chore(ai): add fastapi/uvicorn/openai-agents deps + package skeleton"
```

---

## Task 2: 地理定位工具 (geo.py)

**Files:**
- Create: `spatial_analyse/tools/geo.py`
- Test: `spatial_analyse/tests/test_tools.py`

**Interfaces:**
- Produces:
  - `resolve_place(place: str) -> dict` — 纯函数(可注入 geocoder), 返回 `{"found": bool, "name": str, "lon": float|None, "lat": float|None, "message": str}`
  - `geo_locate` — `@function_tool` 装饰的 Agent 工具, 签名 `geo_locate(place: str) -> str` (返回 JSON 字符串)

- [ ] **Step 1: 写失败测试**

`spatial_analyse/tests/test_tools.py`:
```python
import json
from spatial_analyse.tools.geo import resolve_place


class _FakeLoc:
    def __init__(self, lon, lat):
        self.longitude = lon
        self.latitude = lat


def test_resolve_place_found():
    # 注入假 geocoder: 返回九寨沟坐标
    def fake_geocode(q, **kw):
        return _FakeLoc(103.918, 33.262)

    r = resolve_place("九寨沟", geocode=fake_geocode)
    assert r["found"] is True
    assert abs(r["lon"] - 103.918) < 1e-6
    assert abs(r["lat"] - 33.262) < 1e-6
    assert r["name"] == "九寨沟"


def test_resolve_place_not_found():
    def fake_geocode(q, **kw):
        return None

    r = resolve_place("不存在的地方xyz", geocode=fake_geocode)
    assert r["found"] is False
    assert r["lon"] is None
    assert "未" in r["message"] or "not" in r["message"].lower()
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd spatial_analyse && uv run pytest tests/test_tools.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'spatial_analyse.tools.geo'`

- [ ] **Step 3: 实现 geo.py**

`spatial_analyse/tools/geo.py`:
```python
"""地理定位工具: 地名 -> 经纬度。用 geopy Nominatim。"""
import json

from agents import function_tool


def resolve_place(place: str, geocode=None) -> dict:
    """地名 -> 经纬度 (纯函数, geocode 可注入便于测试)。

    geocode: 可调用, 入参 (query, **kw), 返回带 .longitude/.latitude 的对象或 None。
    默认用 geopy Nominatim。
    """
    if geocode is None:
        from geopy.geocoders import Nominatim
        geocode = Nominatim(user_agent="gcsj-disaster-agent", timeout=10).geocode

    try:
        loc = geocode(place)
    except Exception as e:  # 网络/限流等
        return {"found": False, "name": place, "lon": None, "lat": None,
                "message": f"定位服务异常: {e}"}

    if loc is None:
        return {"found": False, "name": place, "lon": None, "lat": None,
                "message": f"未找到地点「{place}」, 请换个更具体的说法 (如加上省市)。"}

    return {"found": True, "name": place,
            "lon": float(loc.longitude), "lat": float(loc.latitude),
            "message": "ok"}


@function_tool
def geo_locate(place: str) -> str:
    """把地名 (景点/城市/区县) 解析为经纬度坐标。

    Args:
        place: 中文地名, 如「九寨沟」「峨眉山」「成都都江堰」。
    Returns:
        JSON 字符串: {found, name, lon, lat, message}
    """
    return json.dumps(resolve_place(place), ensure_ascii=False)
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd spatial_analyse && uv run pytest tests/test_tools.py -v`
Expected: PASS (2 passed)

- [ ] **Step 5: Commit**

```bash
git add spatial_analyse/tools/geo.py spatial_analyse/tests/test_tools.py
git commit -m "feat(ai): add geo_locate tool (place -> coords)"
```

---

## Task 3: GIS 历史灾害查询工具 (gis_query.py)

**Files:**
- Create: `spatial_analyse/tools/gis_query.py`
- Test: `spatial_analyse/tests/test_tools.py` (追加)

**Interfaces:**
- Consumes: `DB_DSN` 常量 (本任务内定义)
- Produces:
  - `summarize_disasters(rows: list[dict]) -> dict` — 纯函数, 把 `biz_disaster_eval` 行聚合为 `{total_days, risk_days, by_level: {1..4}, by_month: {1..12}, dominant: str, max_comp_level: int}`
  - `query_station_disasters` — `@function_tool`, 签名 `query_station_disasters(lon: float, lat: float) -> str` (返回 JSON 字符串, 含就近站信息 + 聚合)

**说明:** `by_month` 用 `comp_level>=1` 的风险日按月计数, 支撑「9 月是否高发」这类带时间的提问; `dominant` 取四类灾种 (landslide/mudslide/freezethaw/collapse) 在风险日中达标次数最多者的中文名。

- [ ] **Step 1: 写失败测试 (追加到 test_tools.py)**

在 `spatial_analyse/tests/test_tools.py` 末尾追加:
```python
from spatial_analyse.tools.gis_query import summarize_disasters


def test_summarize_disasters_counts_levels_and_months():
    rows = [
        # obs_date, comp_level, landslide, mudslide, freezethaw, collapse
        {"obs_date_month": 9, "comp_level": 4, "landslide_level": 2,
         "mudslide_level": 4, "freezethaw_level": 0, "collapse_level": 1},
        {"obs_date_month": 9, "comp_level": 3, "landslide_level": 3,
         "mudslide_level": 1, "freezethaw_level": 0, "collapse_level": 0},
        {"obs_date_month": 1, "comp_level": 0, "landslide_level": 0,
         "mudslide_level": 0, "freezethaw_level": 0, "collapse_level": 0},
    ]
    s = summarize_disasters(rows)
    assert s["total_days"] == 3
    assert s["risk_days"] == 2                 # comp_level>=1 的天数
    assert s["by_level"][4] == 1
    assert s["by_level"][3] == 1
    assert s["by_month"][9] == 2               # 9 月 2 个风险日
    assert s["by_month"][1] == 0               # 1 月无风险日
    assert s["max_comp_level"] == 4
    # 泥石流(4)+滑坡(3) 各出现, 主导取达标(>=1)次数最多: 滑坡2次 vs 泥石流2次 -> 取风险总和更高者
    assert s["dominant"] in ("滑坡", "泥石流")


def test_summarize_disasters_empty():
    s = summarize_disasters([])
    assert s["total_days"] == 0
    assert s["risk_days"] == 0
    assert s["max_comp_level"] == 0
    assert s["dominant"] == "无"
```

- [ ] **Step 2: 运行确认失败**

Run: `cd spatial_analyse && uv run pytest tests/test_tools.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'spatial_analyse.tools.gis_query'`

- [ ] **Step 3: 实现 gis_query.py**

`spatial_analyse/tools/gis_query.py`:
```python
"""GIS 历史灾害查询工具: 经纬度 -> 就近气象站 -> biz_disaster_eval 历史聚合。"""
import json

import psycopg2
import psycopg2.extras

DB_DSN = "host=localhost port=5432 dbname=gcsj user=gcsj password=gcsj123"

# 四类灾种列 -> 中文名
_HAZARD_COLS = {
    "landslide_level": "滑坡",
    "mudslide_level": "泥石流",
    "freezethaw_level": "冻融滑坡",
    "collapse_level": "坡面崩塌",
}


def summarize_disasters(rows: list) -> dict:
    """把 biz_disaster_eval 行聚合为统计摘要 (纯函数)。

    每行需含: obs_date_month(int 1-12), comp_level(int),
    landslide_level/mudslide_level/freezethaw_level/collapse_level(int)。
    """
    by_level = {1: 0, 2: 0, 3: 0, 4: 0}
    by_month = {m: 0 for m in range(1, 13)}
    hazard_weight = {name: 0 for name in _HAZARD_COLS.values()}
    risk_days = 0
    max_comp = 0

    for r in rows:
        cl = int(r.get("comp_level") or 0)
        max_comp = max(max_comp, cl)
        if cl >= 1:
            risk_days += 1
            if cl in by_level:
                by_level[cl] += 1
            m = int(r.get("obs_date_month") or 0)
            if 1 <= m <= 12:
                by_month[m] += 1
            # 主导灾种: 累加各灾种等级作为权重
            for col, name in _HAZARD_COLS.items():
                hazard_weight[name] += int(r.get(col) or 0)

    dominant = "无"
    if risk_days > 0:
        dominant = max(hazard_weight, key=hazard_weight.get)
        if hazard_weight[dominant] == 0:
            dominant = "无"

    return {
        "total_days": len(rows),
        "risk_days": risk_days,
        "by_level": by_level,
        "by_month": by_month,
        "max_comp_level": max_comp,
        "dominant": dominant,
    }


def nearest_station(lon: float, lat: float, dsn: str = DB_DSN) -> dict:
    """按经纬度就近找站 (PostGIS 距离排序)。返回 {code, name, dist_km} 或 {}。"""
    sql = """
        SELECT code, name,
               ST_Distance(location::geography,
                           ST_SetSRID(ST_MakePoint(%s, %s), 4326)::geography) / 1000.0 AS dist_km
        FROM gis.gis_weather_station
        ORDER BY location <-> ST_SetSRID(ST_MakePoint(%s, %s), 4326)
        LIMIT 1
    """
    conn = psycopg2.connect(dsn)
    try:
        with conn.cursor(cursor_factory=psycopg2.extras.RealDictCursor) as cur:
            cur.execute(sql, (lon, lat, lon, lat))
            row = cur.fetchone()
    finally:
        conn.close()
    if not row:
        return {}
    return {"code": row["code"], "name": row["name"],
            "dist_km": round(float(row["dist_km"]), 1)}


def _fetch_eval_rows(station_code: str, dsn: str = DB_DSN) -> list:
    sql = """
        SELECT EXTRACT(MONTH FROM obs_date)::int AS obs_date_month,
               comp_level, landslide_level, mudslide_level,
               freezethaw_level, collapse_level
        FROM biz.biz_disaster_eval
        WHERE station_code = %s
    """
    conn = psycopg2.connect(dsn)
    try:
        with conn.cursor(cursor_factory=psycopg2.extras.RealDictCursor) as cur:
            cur.execute(sql, (station_code,))
            return cur.fetchall()
    finally:
        conn.close()


@function_tool_wrapper := None  # placeholder to avoid accidental import error
```

**注意:** 上一行 `@function_tool_wrapper := None` 是错误占位, **删掉它**, 用下面正确的工具定义结尾:

```python
from agents import function_tool


@function_tool
def query_station_disasters(lon: float, lat: float) -> str:
    """查询给定经纬度就近气象站的历史灾害统计 (2020-2023)。

    Args:
        lon: 经度
        lat: 纬度
    Returns:
        JSON 字符串: {station: {code,name,dist_km}, summary: {...}} 或 {error}
    """
    try:
        st = nearest_station(lon, lat)
        if not st:
            return json.dumps({"error": "附近无气象站数据"}, ensure_ascii=False)
        rows = _fetch_eval_rows(st["code"])
        summary = summarize_disasters(rows)
        return json.dumps({"station": st, "summary": summary}, ensure_ascii=False)
    except Exception as e:
        return json.dumps({"error": f"查询失败: {e}"}, ensure_ascii=False)
```

- [ ] **Step 4: 运行确认通过 (纯函数部分)**

Run: `cd spatial_analyse && uv run pytest tests/test_tools.py -v`
Expected: PASS (4 passed) — `nearest_station`/`_fetch_eval_rows` 需真库, 不在单测覆盖, 由 Task 7 集成验证。

- [ ] **Step 5: Commit**

```bash
git add spatial_analyse/tools/gis_query.py spatial_analyse/tests/test_tools.py
git commit -m "feat(ai): add query_station_disasters tool (nearest station + history summary)"
```

---

## Task 4: 静态知识库工具 (knowledge.py)

**Files:**
- Create: `spatial_analyse/tools/knowledge.py`
- Test: `spatial_analyse/tests/test_tools.py` (追加)

**Interfaces:**
- Produces:
  - `lookup_knowledge(topic: str) -> str` — 纯函数, 按灾种关键词匹配返回科普+出行防范文本; 无匹配返回全部灾种简介
  - `disaster_kb` — `@function_tool`, 签名 `disaster_kb(topic: str) -> str`

- [ ] **Step 1: 写失败测试 (追加)**

在 `test_tools.py` 末尾追加:
```python
from spatial_analyse.tools.knowledge import lookup_knowledge


def test_lookup_knowledge_matches_hazard():
    text = lookup_knowledge("泥石流")
    assert "泥石流" in text
    assert len(text) > 20


def test_lookup_knowledge_fallback():
    text = lookup_knowledge("完全不相关的词")
    # 无匹配 -> 返回总览, 至少包含多个灾种名
    assert "暴雨" in text and "滑坡" in text
```

- [ ] **Step 2: 运行确认失败**

Run: `cd spatial_analyse && uv run pytest tests/test_tools.py -v`
Expected: FAIL — `ModuleNotFoundError: ...knowledge`

- [ ] **Step 3: 实现 knowledge.py**

`spatial_analyse/tools/knowledge.py`:
```python
"""静态灾害知识库工具: 灾种科普 + 出行防范建议 (不做向量检索)。"""
from agents import function_tool

# 灾种 -> (科普, 出行防范)。含别名以便关键词匹配。
_KB = {
    ("暴雨", "降雨", "洪水"): (
        "暴雨指短时强降水, 四川盆地与山区夏季 (6-9月) 汛期高发, 易引发城市内涝、"
        "山洪及次生地质灾害。",
        "出行防范: 汛期避免前往河谷、低洼地带露营; 关注气象预警, 暴雨橙/红色预警时"
        "推迟山区行程; 遇积水路段勿强行通过。"
    ),
    ("滑坡", "山体滑坡"): (
        "滑坡是斜坡岩土体在重力与降雨作用下整体下滑, 前期有效降雨累积后风险陡增, "
        "川西山区雨季多发。",
        "出行防范: 雨后勿在陡坡、公路切坡下方停留; 发现坡体裂缝、渗水、树木倾斜等"
        "征兆立即远离; 选择成熟景区步道, 勿走废弃山路。"
    ),
    ("泥石流",): (
        "泥石流是山区沟谷中含大量泥沙石块的洪流, 突发性强、破坏力大, 强降雨与前期"
        "降雨叠加时最危险。",
        "出行防范: 强降雨期间远离沟谷、河道; 露营选高处远离沟口; 听到轰鸣声或见"
        "沟水突然变浑, 立即向沟谷两侧高处逃离, 不要顺沟跑。"
    ),
    ("冻融", "冻融滑坡"): (
        "冻融滑坡由昼夜温差反复冻结融化导致岩土体松动, 高海拔地区早春 (最低温<0且"
        "最高温>0) 易发。",
        "出行防范: 高原早春徒步注意昼夜温差下的落石与坡面松动; 清晨解冻时段远离"
        "陡崖下方。"
    ),
    ("崩塌", "坡面崩塌", "落石"): (
        "坡面崩塌指陡坡岩块突然脱落坠落, 高湿度、强降雨会加剧, 常发生于公路沿线陡崖。",
        "出行防范: 勿在陡崖、危岩下方久留拍照; 雨中雨后山区公路警惕落石; 遵守景区"
        "封路提示。"
    ),
}


def lookup_knowledge(topic: str) -> str:
    """按关键词匹配灾种科普; 无匹配返回全部灾种简介 (纯函数)。"""
    if topic:
        for aliases, (intro, advice) in _KB.items():
            if any(a in topic for a in aliases):
                name = aliases[0]
                return f"【{name}】{intro}\n{advice}"
    # fallback: 全部灾种总览
    parts = []
    for aliases, (intro, advice) in _KB.items():
        parts.append(f"【{aliases[0]}】{intro} {advice}")
    return "\n".join(parts)


@function_tool
def disaster_kb(topic: str) -> str:
    """查询灾害科普知识与出行防范建议。

    Args:
        topic: 灾种关键词, 如「泥石流」「暴雨」「滑坡」; 传空或未知词返回全部灾种总览。
    Returns:
        科普 + 出行防范文本。
    """
    return lookup_knowledge(topic)
```

- [ ] **Step 4: 运行确认通过**

Run: `cd spatial_analyse && uv run pytest tests/test_tools.py -v`
Expected: PASS (6 passed)

- [ ] **Step 5: Commit**

```bash
git add spatial_analyse/tools/knowledge.py spatial_analyse/tests/test_tools.py
git commit -m "feat(ai): add disaster_kb tool (static hazard knowledge + travel advice)"
```

---

## Task 5: Agent 组装与流式运行器 (agent_core.py)

**Files:**
- Create: `spatial_analyse/agent_core.py`

**Interfaces:**
- Consumes: `geo_locate`, `query_station_disasters`, `disaster_kb` (Task 2-4)
- Produces:
  - `build_agent() -> Agent` — 组装好工具与系统提示词的 Agent
  - `async stream_reply(messages: list[dict]) -> AsyncIterator[dict]` — 输入 `[{role, content}...]`, 逐个 yield `{"type": "tool"|"token"|"done"|"error", ...}` 事件

**说明:** DashScope 走 OpenAI 兼容端点, 用 Agents SDK 的 `OpenAIChatCompletionsModel` + 自定义 `AsyncOpenAI` 客户端接入。流式用 `Runner.run_streamed(...)` 的 `stream_events()`: `raw_response_event` 增量文本 → `token` 帧; `run_item_stream_event` 且 item 为 tool_call → `tool` 帧。

- [ ] **Step 1: 实现 agent_core.py**

`spatial_analyse/agent_core.py`:
```python
"""组装灾害出行风险研判 Agent, 提供流式回复生成器。"""
import os

from dotenv import load_dotenv
from openai import AsyncOpenAI
from agents import Agent, Runner, OpenAIChatCompletionsModel
from agents.models.openai_responses import OpenAIResponsesModel  # noqa: F401 (占位, 未用)

from spatial_analyse.tools.geo import geo_locate
from spatial_analyse.tools.gis_query import query_station_disasters
from spatial_analyse.tools.knowledge import disaster_kb

load_dotenv(override=True)

_MODEL_ID = os.getenv("DASHSCOPE_MODEL_ID")
_API_KEY = os.getenv("DASHSCOPE_API_KEY")
_BASE_URL = "https://dashscope.aliyuncs.com/compatible-mode/v1"

_SYSTEM_PROMPT = (
    "你是「四川灾害出行风险研判助手」。用户会告诉你想去的地点和时间, 你的任务是结合"
    "该地就近气象站的历史灾害数据, 给出针对性的出行风险研判与建议, 而不是泛泛的科普。\n"
    "工作流程 (自行判断调用哪些工具):\n"
    "1. 用 geo_locate 把地名解析为经纬度;\n"
    "2. 用 query_station_disasters 查该坐标就近站点的历史灾害统计 (含按月份分布);\n"
    "3. 如需科普/防范细节, 用 disaster_kb 查对应灾种;\n"
    "最后综合输出: 该地历史上哪些季节/月份、哪种灾害风险偏高 (引用 by_month 与"
    " max_comp_level 等数据), 结合用户出行月份给出是否适宜、注意事项。\n"
    "语气面向普通游客, 简洁、务实、给可执行建议。若地点无法定位, 礼貌请用户补充更"
    "具体的地名。用中文回答。"
)


def _make_model() -> OpenAIChatCompletionsModel:
    client = AsyncOpenAI(api_key=_API_KEY, base_url=_BASE_URL)
    return OpenAIChatCompletionsModel(model=_MODEL_ID, openai_client=client)


def build_agent() -> Agent:
    return Agent(
        name="灾害出行风险研判助手",
        instructions=_SYSTEM_PROMPT,
        model=_make_model(),
        tools=[geo_locate, query_station_disasters, disaster_kb],
    )


async def stream_reply(messages: list):
    """输入对话历史 [{role, content}...], 逐个 yield 事件 dict。

    事件: {"type":"tool","name":str,"status":str}
          {"type":"token","text":str}
          {"type":"done"}
          {"type":"error","message":str}
    """
    if not _API_KEY or not _MODEL_ID:
        yield {"type": "error", "message": "AI 服务未配置 (缺少 DASHSCOPE_API_KEY/MODEL_ID)"}
        return

    try:
        agent = build_agent()
        # Agents SDK 接受字符串或消息列表作为 input; 这里传对话历史列表
        result = Runner.run_streamed(agent, input=messages)
        async for event in result.stream_events():
            if event.type == "raw_response_event":
                data = getattr(event, "data", None)
                delta = getattr(data, "delta", None)
                if delta:
                    yield {"type": "token", "text": delta}
            elif event.type == "run_item_stream_event":
                item = getattr(event, "item", None)
                if item is not None and getattr(item, "type", "") == "tool_call_item":
                    raw = getattr(item, "raw_item", None)
                    tool_name = getattr(raw, "name", "工具")
                    status = {
                        "geo_locate": "正在定位地点",
                        "query_station_disasters": "正在查询就近站点历史灾害",
                        "disaster_kb": "正在查询灾害科普",
                    }.get(tool_name, f"正在调用 {tool_name}")
                    yield {"type": "tool", "name": tool_name, "status": status}
        yield {"type": "done"}
    except Exception as e:
        yield {"type": "error", "message": f"AI 生成失败: {e}"}
```

**注意:** 上面 `from agents.models.openai_responses import OpenAIResponsesModel` 这一行是多余的, **删掉它**。保留 `from agents import Agent, Runner, OpenAIChatCompletionsModel`。

- [ ] **Step 2: 冒烟测试 — Agent 能构建**

Run: `cd spatial_analyse && uv run python -c "from spatial_analyse.agent_core import build_agent; a=build_agent(); print(a.name, len(a.tools))"`
Expected: 输出 `灾害出行风险研判助手 3` (不实际调用 LLM, 只验证组装)

- [ ] **Step 3: 事件流字段冒烟 (需 .env 配好 Key + 数据库运行)**

Run:
```bash
cd spatial_analyse && uv run python -c "
import asyncio
from spatial_analyse.agent_core import stream_reply
async def main():
    types=set()
    async for ev in stream_reply([{'role':'user','content':'我9月想去九寨沟玩安全吗'}]):
        types.add(ev['type'])
        if ev['type']=='token': print(ev['text'], end='')
    print('\n--- event types:', types)
asyncio.run(main())
"
```
Expected: 打印一段中文研判文本, 末尾 event types 至少含 `{'tool','token','done'}`。若 Key/库未就绪则应看到 `error` 事件而非 traceback。

- [ ] **Step 4: Commit**

```bash
git add spatial_analyse/agent_core.py
git commit -m "feat(ai): assemble travel-risk agent with streaming reply generator"
```

---

## Task 6: FastAPI 服务 (api_server.py)

**Files:**
- Create: `spatial_analyse/api_server.py`

**Interfaces:**
- Consumes: `stream_reply` (Task 5)
- Produces: HTTP 服务 — `GET /ai/health` → `{"status":"ok"}`; `POST /ai/chat` (body `{"messages":[{role,content}...]}`) → `text/event-stream`, 每帧 `data: {json}\n\n`

- [ ] **Step 1: 实现 api_server.py**

`spatial_analyse/api_server.py`:
```python
"""AI 灾害助手 FastAPI 服务。

启动: uv run uvicorn spatial_analyse.api_server:app --port 8000
"""
import json

from fastapi import FastAPI
from fastapi.responses import StreamingResponse
from pydantic import BaseModel

from spatial_analyse.agent_core import stream_reply

app = FastAPI(title="AI 灾害助手")


class ChatMessage(BaseModel):
    role: str
    content: str


class ChatRequest(BaseModel):
    messages: list[ChatMessage]


@app.get("/ai/health")
async def health():
    return {"status": "ok"}


@app.post("/ai/chat")
async def chat(req: ChatRequest):
    messages = [{"role": m.role, "content": m.content} for m in req.messages]

    async def event_gen():
        try:
            async for ev in stream_reply(messages):
                yield f"data: {json.dumps(ev, ensure_ascii=False)}\n\n"
        except Exception as e:  # 兜底: 任何未捕获异常也以 error 帧收尾
            err = {"type": "error", "message": f"服务异常: {e}"}
            yield f"data: {json.dumps(err, ensure_ascii=False)}\n\n"

    return StreamingResponse(event_gen(), media_type="text/event-stream")
```

- [ ] **Step 2: 启动服务**

Run (后台): `cd spatial_analyse && uv run uvicorn spatial_analyse.api_server:app --port 8000`
Expected: uvicorn 启动, 监听 8000, 无导入错误。

- [ ] **Step 3: 验证 health**

Run: `curl -s http://localhost:8000/ai/health`
Expected: `{"status":"ok"}`

- [ ] **Step 4: 验证 SSE 流 (需 Key + 库)**

Run: `curl -N -s -X POST http://localhost:8000/ai/chat -H "Content-Type: application/json" -d '{"messages":[{"role":"user","content":"我9月想去九寨沟玩安全吗"}]}'`
Expected: 多行 `data: {...}`, 依次出现 `type":"tool"`、`type":"token"`、结尾 `type":"done"`。

- [ ] **Step 5: Commit**

```bash
git add spatial_analyse/api_server.py
git commit -m "feat(ai): add FastAPI SSE server (/ai/chat, /ai/health)"
```

---

## Task 7: 前端 SSE 客户端 + Vite proxy (agent.js)

**Files:**
- Create: `frontend/src/api/agent.js`
- Modify: `frontend/vite.config.js`

**Interfaces:**
- Produces:
  - `streamChat(messages, {onTool, onToken, onDone, onError, signal})` — 用 fetch 读 SSE, 逐帧回调。`messages` 为 `[{role,content}...]`。

- [ ] **Step 1: 加 Vite proxy**

在 `frontend/vite.config.js` 的 `proxy` 对象中, `/ws` 之后追加 `/ai`:
```js
        '/ws': {
          target: env.VITE_WS_PROXY_TARGET || 'http://localhost:8085',
          changeOrigin: true,
          ws: true
        },
        '/ai': {
          target: env.VITE_AI_PROXY_TARGET || 'http://localhost:8000',
          changeOrigin: true
        }
```

- [ ] **Step 2: 实现 agent.js**

`frontend/src/api/agent.js`:
```js
/**
 * AI 灾害助手 SSE 流式客户端。
 *
 * 后端 (Python FastAPI :8000, 经 Vite proxy /ai) POST /ai/chat 返回 text/event-stream,
 * 每帧 `data: {json}\n\n`, json.type ∈ {tool, token, done, error}。
 * 用原生 fetch + ReadableStream 读取 (axios 不适合流式)。
 */

/**
 * @param {Array<{role:string,content:string}>} messages 完整对话历史
 * @param {object} cb { onTool, onToken, onDone, onError, signal }
 */
export async function streamChat(messages, { onTool, onToken, onDone, onError, signal } = {}) {
  try {
    const resp = await fetch('/ai/chat', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ messages }),
      signal,
    })
    if (!resp.ok || !resp.body) {
      onError?.(`AI 服务暂不可用 (HTTP ${resp.status})`)
      return
    }

    const reader = resp.body.getReader()
    const decoder = new TextDecoder('utf-8')
    let buf = ''

    while (true) {
      const { value, done } = await reader.read()
      if (done) break
      buf += decoder.decode(value, { stream: true })

      // 按 SSE 帧分隔 (\n\n) 切分
      let idx
      while ((idx = buf.indexOf('\n\n')) !== -1) {
        const frame = buf.slice(0, idx).trim()
        buf = buf.slice(idx + 2)
        if (!frame.startsWith('data:')) continue
        const jsonStr = frame.slice(5).trim()
        if (!jsonStr) continue
        let ev
        try { ev = JSON.parse(jsonStr) } catch { continue }
        if (ev.type === 'tool') onTool?.(ev)
        else if (ev.type === 'token') onToken?.(ev.text || '')
        else if (ev.type === 'done') onDone?.()
        else if (ev.type === 'error') onError?.(ev.message || 'AI 生成失败')
      }
    }
    onDone?.()
  } catch (e) {
    if (e?.name === 'AbortError') return
    onError?.('AI 服务暂不可用')
  }
}
```

- [ ] **Step 3: 验证 proxy 生效 (前端 dev 运行时)**

Run: 启动前端 `cd frontend && npm run dev`, 浏览器 devtools console 执行
`fetch('/ai/health').then(r=>r.json()).then(console.log)`
Expected: 打印 `{status: "ok"}` (证明 `/ai` proxy 通到 Python :8000; 需 Python 服务在跑)

- [ ] **Step 4: Commit**

```bash
git add frontend/src/api/agent.js frontend/vite.config.js
git commit -m "feat(ai): frontend SSE client + /ai vite proxy"
```

---

## Task 8: 前端页面 AgentView.vue + 路由修复

**Files:**
- Create: `frontend/src/views/agent/AgentView.vue`
- Delete: `frontend/src/views/agent/agentView.vue`
- Modify: `frontend/src/router/index.js:26-30`

**Interfaces:**
- Consumes: `streamChat` (Task 7)

- [ ] **Step 1: 删除空占位文件**

Run: `cd frontend && git rm src/views/agent/agentView.vue`
Expected: 删除 0 字节空文件 (若 git 因大小写在 Windows 上不识别, 用 `rm src/views/agent/agentView.vue`)。

- [ ] **Step 2: 修复路由**

`frontend/src/router/index.js` 第 26-30 行整段替换:
```js
      {
        path: 'agent',
        name: 'Agent',
        component: () => import('@/views/agent/AgentView.vue'),
        meta: { title: 'AI灾害助手', icon: 'ChatDotRound', permission: 'dashboard:view' }
      },
```

- [ ] **Step 3: 创建 AgentView.vue**

`frontend/src/views/agent/AgentView.vue`:
```vue
<template>
  <div class="agent-root">
    <header class="agent-head">
      <div class="agent-title">
        <el-icon :size="20"><ChatDotRound /></el-icon>
        <span>AI 灾害助手</span>
        <span class="agent-sub">· 出行风险研判</span>
      </div>
    </header>

    <div class="agent-body" ref="bodyRef">
      <div v-for="(m, i) in messages" :key="i" class="msg" :class="m.role">
        <div class="bubble" :class="{ err: m.error }">
          <span v-if="m.tool" class="tool-hint">🔧 {{ m.tool }}…</span>
          <span class="content" v-text="m.content"></span>
          <span v-if="m.role === 'assistant' && m.streaming && !m.content" class="typing">思考中…</span>
        </div>
      </div>
    </div>

    <div class="agent-quick">
      <button v-for="q in quickAsks" :key="q" class="quick-chip" @click="sendQuick(q)">{{ q }}</button>
    </div>

    <div class="agent-input">
      <el-input
        v-model="draft"
        type="textarea"
        :rows="2"
        resize="none"
        placeholder="例如: 我 9 月想去九寨沟玩, 安全吗?"
        @keydown.enter.exact.prevent="send"
        :disabled="busy"
      />
      <el-button type="primary" :loading="busy" @click="send">发送</el-button>
    </div>
  </div>
</template>

<script setup>
import { ref, nextTick } from 'vue'
import { ChatDotRound } from '@element-plus/icons-vue'
import { streamChat } from '@/api/agent'

const messages = ref([
  { role: 'assistant', content: '你好! 告诉我你想去的地点和时间 (如「我 9 月想去九寨沟玩」), 我会结合当地历史灾害数据帮你研判出行风险。' },
])
const draft = ref('')
const busy = ref(false)
const bodyRef = ref(null)

const quickAsks = [
  '我 9 月想去九寨沟玩, 安全吗?',
  '暑假去峨眉山要注意什么灾害?',
  '雨季去都江堰安全吗?',
]

async function scrollToBottom() {
  await nextTick()
  if (bodyRef.value) bodyRef.value.scrollTop = bodyRef.value.scrollHeight
}

function sendQuick(q) {
  if (busy.value) return
  draft.value = q
  send()
}

async function send() {
  const text = draft.value.trim()
  if (!text || busy.value) return
  draft.value = ''
  busy.value = true

  messages.value.push({ role: 'user', content: text })
  const assistant = { role: 'assistant', content: '', tool: '', streaming: true, error: false }
  messages.value.push(assistant)
  scrollToBottom()

  // 只把 user/assistant 的实质对话发给后端 (去掉本地 UI 字段)
  const history = messages.value
    .filter(m => m.content || m === assistant)
    .slice(0, -1)  // 不含刚 push 的空 assistant
    .map(m => ({ role: m.role, content: m.content }))
    .filter(m => m.content)

  await streamChat(history, {
    onTool: (ev) => { assistant.tool = ev.status; scrollToBottom() },
    onToken: (t) => { assistant.tool = ''; assistant.content += t; scrollToBottom() },
    onDone: () => { assistant.streaming = false; assistant.tool = ''; busy.value = false },
    onError: (msg) => {
      assistant.streaming = false
      assistant.tool = ''
      assistant.error = true
      assistant.content = assistant.content || msg
      busy.value = false
    },
  })
}
</script>

<style scoped>
.agent-root {
  height: 100%;
  display: flex;
  flex-direction: column;
  background: var(--au-bg-surface, #fff);
  border-radius: var(--au-radius-lg, 12px);
  border: 1px solid var(--au-border-subtle, #E5E7EB);
  overflow: hidden;
}
.agent-head {
  padding: 14px 18px;
  border-bottom: 1px solid var(--au-border-subtle, #E5E7EB);
}
.agent-title {
  display: flex; align-items: center; gap: 8px;
  font-size: 16px; font-weight: 700; color: #1E293B;
}
.agent-sub { font-size: 12px; font-weight: 400; color: #94A3B8; }

.agent-body {
  flex: 1; overflow-y: auto; padding: 18px;
  display: flex; flex-direction: column; gap: 14px;
}
.msg { display: flex; }
.msg.user { justify-content: flex-end; }
.msg.assistant { justify-content: flex-start; }
.bubble {
  max-width: 76%;
  padding: 10px 14px;
  border-radius: 12px;
  font-size: 14px; line-height: 1.6;
  white-space: pre-wrap; word-break: break-word;
}
.msg.user .bubble {
  background: linear-gradient(135deg, #2563EB, #06B6D4);
  color: #fff;
}
.msg.assistant .bubble {
  background: #F1F5F9; color: #1E293B;
}
.bubble.err { background: #FEF2F2; color: #DC2626; }
.tool-hint { display: block; font-size: 12px; color: #64748B; margin-bottom: 2px; }
.typing { color: #94A3B8; }

.agent-quick {
  display: flex; gap: 8px; flex-wrap: wrap;
  padding: 0 18px 8px;
}
.quick-chip {
  padding: 5px 12px; font-size: 12px;
  border: 1px solid #E5E7EB; border-radius: 999px;
  background: #fff; color: #475569; cursor: pointer;
  transition: all .15s;
}
.quick-chip:hover { background: #EFF6FF; border-color: #93C5FD; color: #1D4ED8; }

.agent-input {
  display: flex; gap: 10px; align-items: flex-end;
  padding: 12px 18px 16px;
  border-top: 1px solid var(--au-border-subtle, #E5E7EB);
}
.agent-input :deep(.el-textarea) { flex: 1; }
</style>
```

- [ ] **Step 4: 端到端手动验证**

前置: Python 服务 (`:8000`) 运行中 + Java 后端可登录 + 数据库有数据。
1. `cd frontend && npm run dev`, 登录后侧边栏点「AI灾害助手」。
2. 确认菜单项正常渲染 (icon 不报错), 页面打开无控制台错误。
3. 点快捷提问「我 9 月想去九寨沟玩, 安全吗?」。
Expected: 先看到「🔧 正在定位地点…」→「🔧 正在查询就近站点历史灾害…」等提示, 随后助手气泡逐字流式输出一段结合历史数据的出行研判; 结束后输入框可再次输入 (多轮)。

- [ ] **Step 5: 降级验证**

停掉 Python 服务, 再发一条消息。
Expected: 助手气泡显示红色「AI 服务暂不可用」, 页面不卡死, 可继续操作。

- [ ] **Step 6: Commit**

```bash
git add frontend/src/views/agent/AgentView.vue frontend/src/router/index.js
git commit -m "feat(ai): AI disaster assistant page + fix /agent route & icon"
```

---

## Self-Review

**Spec 覆盖检查:**
- 删除空 `/agent` 占位 → Task 8 Step 1 ✅
- Python FastAPI 独立服务 → Task 6 ✅
- `@function_tool` 三工具 (geo/gis/kb) → Task 2/3/4 ✅
- 结合站点历史灾害的针对性出行研判 (含月份分布) → Task 3 `summarize_disasters` by_month + Task 5 系统提示词 ✅
- 静态知识库 (不做向量检索) → Task 4 ✅
- SSE 流式 + 多轮对话 → Task 5/6/7/8 (前端全量历史) ✅
- 错误处理 (Key 缺失/DB 断/定位失败/服务未启) → Task 5 error 帧 + Task 7 降级 + Task 8 Step 5 ✅
- Vite proxy `/ai` → Task 7 ✅
- 路由 icon 非法名修复 → Task 8 ✅

**占位符扫描:** 无 TBD/TODO; Task 3 与 Task 5 各标注了一处「删掉这行」的错误占位提醒 (真实文件中不应保留), 已明确指出。

**类型一致性:** `stream_reply` 事件 dict 的 `type`/`text`/`name`/`status`/`message` 字段在 Task 5 定义, Task 6 原样透传, Task 7 `streamChat` 回调按同名字段解析, Task 8 消费一致 ✅。工具函数名 `geo_locate`/`query_station_disasters`/`disaster_kb` 在 Task 2-4 定义, Task 5 import 一致 ✅。

**测试框架说明:** 项目原无测试框架。本计划仅为纯函数逻辑 (geo 解析/灾害聚合/知识匹配) 引入轻量 pytest (Task 1 加 dev 依赖); 涉及真库/LLM/流式的部分用 curl + 浏览器手动验证, 不强行搭建重型测试栈 —— 契合现有代码库习惯。
